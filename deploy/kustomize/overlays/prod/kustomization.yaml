apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - pdb.yaml
  - networkpolicy.yaml
  # Optional: uncomment to enable external access
  # - ingress.yaml
  # Optional: uncomment to enable horizontal pod autoscaling
  # - hpa.yaml

patches:
  - path: deployment-patch.yaml
