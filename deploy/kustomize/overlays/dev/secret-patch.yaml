# Dev/test secret values for Kind cluster integration testing.
# These are NOT production secrets — they are test-only placeholders.
apiVersion: v1
kind: Secret
metadata:
  name: trace-report-secrets
stringData:
  SIGNOZ_ENDPOINT: "http://signoz:8080"
  SIGNOZ_JWT_SECRET: "test-secret-key-for-integration"
  SIGNOZ_API_KEY: ""
